# 2026-08-09

## Flip to Shhh — About 弹窗升级为全屏 AboutScreen
- 只改 `MainActivity.kt`（UI 表现层），未碰 Service/BootReceiver。
- 前置修复：删除了 L1895-2200 孤儿重复代码块（无函数外壳的设置面板副本，导致基线编译失败），非本次需求文档内容但为编译前提。
- 导航：`showAboutDialog` → `showAboutScreen`，`AnimatedContent`（slide+fade）整体切换；`onShowAbout` 先关 sheet 再开页面；AboutScreen 内置 `BackHandler`。
- 新增 `AboutScreen`：顶部栏/品牌区（alpha pill 无 v 前缀）/设备支持卡/技术亮点卡（默认 2 条，animateContentSize 展开 6 条 + ChevronRight 旋转动画）/隐私卡/页脚。白卡 26dp 圆角无描边。
- AppStrings 新增 18 个 key（含文档遗漏的 `nav_back`），三语齐全。
- 补了 `animateFloatAsState`、`slideIn/OutHorizontally`、`Modifier.rotate` import。
- `./gradlew assembleDebug` 通过。
- 注意：页顶大标题按文档指定用 `about_app_title` key，显示为 "Flip to Shhh" 而非「关于」；若想要「关于」可改用现有 `group_about` key。
